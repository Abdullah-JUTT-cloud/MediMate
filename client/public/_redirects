# ─── SEO / crawler-critical files ───────────────────────────────────────────
# These must reach Googlebot, social crawlers and Link Preview bots as real
# files — never rewritten to index.html by the SPA catch-all below.
/robots.txt   /robots.txt   200
/sitemap.xml  /sitemap.xml  200
/og-image.png /og-image.png 200

# ─── SPA fallback ───────────────────────────────────────────────────────────
/* /index.html 200
